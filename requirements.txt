# EigenWave-ASR Dependencies
# Install: pip install -r requirements.txt

torch>=2.0.0
torchaudio>=2.0.0

# Pinned for cross-platform compatibility
numpy>=1.24.0,<2.1.0
pyctcdecode>=0.5.0

# Optional (only needed for --tune mode)
# optuna>=3.0.0,<4.0.0

# KenLM (may need C++ build tools on some platforms)
# pip install https://github.com/kpu/kenlm/archive/master.zip
